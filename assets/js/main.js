(()=>{const root=document.documentElement,theme=document.querySelector('.theme'),trigger=document.querySelector('.hamb'),menu=document.querySelector('#menu');const close=()=>{menu?.classList.remove('open');trigger?.setAttribute('aria-expanded','false');document.body.style.overflow=''};theme?.setAttribute('aria-pressed',String(root.dataset.theme==='dark'));theme?.addEventListener('click',()=>{const next=root.dataset.theme==='dark'?'light':'dark';root.dataset.theme=next;localStorage.setItem('hard-theme',next);theme.setAttribute('aria-pressed',String(next==='dark'));theme.setAttribute('aria-label',next==='dark'?'Switch to light theme':'Switch to dark theme')});trigger?.addEventListener('click',()=>{const open=!menu.classList.contains('open');menu.classList.toggle('open',open);trigger.setAttribute('aria-expanded',String(open));document.body.style.overflow=open?'hidden':''});menu?.addEventListener('click',e=>{if(e.target.closest('a'))close()});addEventListener('keydown',e=>{if(e.key==='Escape')close()});addEventListener('resize',()=>{if(innerWidth>850)close()});document.querySelectorAll('section[id]').forEach(s=>s.style.scrollMarginTop='90px');if(!matchMedia('(prefers-reduced-motion: reduce)').matches&&'IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('seen');observer.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.v2-reveal').forEach(el=>observer.observe(el))}})();
